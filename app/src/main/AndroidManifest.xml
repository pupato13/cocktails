<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hautipua.android.cocktails">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTheme.CocktailsNoActionBar">
        <activity android:name=".activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".activity.CocktailsActivity" />
        <activity android:name=".activity.CocktailDetailActivity" />
        <activity android:name=".activity.SpiritActivity" />
        <activity android:name=".activity.CocktailsBySpiritActivity" />
        <activity android:name=".activity.FavouritesActivity"></activity>
    </application>

</manifest>